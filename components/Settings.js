import { StyleSheet, View, Text } from "react-native"
export default function Settings() {
    return (
        <View>
            <Text style={{textAlign: 'center', fontSize: 35, fontWeight: 'bold', paddingTop: 400}}>Settings</Text>
        </View>
    )
}

const styles= StyleSheet.create({
    
})